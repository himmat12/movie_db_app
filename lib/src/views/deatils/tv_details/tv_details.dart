import 'package:flutter/material.dart';

class TvDetails extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("tv details"),
      ),
    );
  }
}
